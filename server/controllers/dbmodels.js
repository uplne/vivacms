var config     = require('../config'),
    program    = require(config.paths.server + '/models/program'),
    halloffame = require(config.paths.server + '/models/halloffame'),
    kontakt    = require(config.paths.server + '/models/kontakt'),
    newsletter = require(config.paths.server + '/models/newsletter'),
    pressnews  = require(config.paths.server + '/models/pressnews'),
    accounts   = require(config.paths.server + '/models/accounts');
